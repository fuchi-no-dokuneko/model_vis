edit in ammend2.md 1) for large model , do not do one big pass; READ and detect the block that is same and only do forward past on unique layer(some model like qwen have moe later that is different from other and need detection). YOU can accheive it by careful of init parapmeter with layer by layer or even operation by operation ; also use the init parameter to set the model init config size but do not download weight   2)Need KEEP both config ( current approch + offical hf config) . use browser tool to search the offical hf repo and then download config . this will solve Repository authority problem you mention. use a config file to preserve mapping and serch the repo to make make a version1 that is runnable in full pipline, then  i will review when necessary and change the link. I agree in Version pinning , Licensing. for  Security , only download config. save my word in ammend2.txt . ANy more question and decision?
